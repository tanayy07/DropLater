# API service
PORT=3000
MONGODB_URI=mongodb://localhost:27017/droplater
REDIS_URL=redis://localhost:6379

# Simple admin security (used later in Step 3)
ADMIN_TOKEN=changeme
RATE_LIMIT_PER_MINUTE=60

# Sink service
SINK_PORT=4000
SINK_FORCE_ERROR=false

# Worker (used later in Step 4)
DELIVERY_BACKOFF_MS=1000,5000,25000
MAX_DELIVERY_ATTEMPTS=3
